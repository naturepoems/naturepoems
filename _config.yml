title: Nature Poems
email: 
description: >- # this means to ignore newlines until "baseurl:"
  .
baseurl: "" # the subpath of your site, e.g. /blog
url: "" # the base hostname & protocol for your site, e.g. http://example.com
github_username:  borncamp

# Build settings
theme: just-the-docs
plugins:
 # - jekyll-feed

collections:
  authors:
    output: true
    permalink: /authors/:name/

just_the_docs:
  search:
    enabled: true
    token_splitter_regex: '[\s/]+'
    preprocessors:
      - 'title'
      - 'permalink'
      - 'content'

